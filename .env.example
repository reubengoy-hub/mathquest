# Para desarrollo local (SQLite, no necesita configuración adicional)
PORT=3000
SESSION_SECRET=cambia_esto_por_algo_secreto

# Para producción en Render.com (descomenta y rellena con la URL de Neon)
# DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require
